import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function BackupPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Backup</CardTitle>
        <CardDescription>Manage your data backups here.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Backup and restore interface will be here.</p>
      </CardContent>
    </Card>
  );
}
