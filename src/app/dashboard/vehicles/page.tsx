import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function VehiclesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Vehicles</CardTitle>
        <CardDescription>Manage your vehicles here.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Vehicle management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
